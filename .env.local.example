# Supabase Configuration
# Copy this file to .env.local and fill in your values from Vercel environment variables

# Public keys (required for client-side authentication)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Server-side keys (optional, for server-side operations)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
# SUPABASE_JWT_SECRET=your_jwt_secret

# Database connection (optional, if needed for direct database access)
# POSTGRES_URL_NON_POOLING=your_postgres_url
# POSTGRES_USER=your_postgres_user
# POSTGRES_HOST=your_postgres_host
# POSTGRES_PASSWORD=your_postgres_password
# POSTGRES_DATABASE=your_postgres_database
